{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "state.schema.json",
  "$comment": "See _rationale.yaml for component rationale",
  "title": "Work Unit State",
  "description": "Lifecycle and progress tracking for a Furrow work unit. Furrow-exclusive write — agents never write this file directly.",
  "type": "object",
  "required": [
    "name",
    "title",
    "description",
    "step",
    "step_status",
    "steps_sequence",
    "deliverables",
    "gates",
    "force_stop_at",
    "branch",
    "mode",
    "base_commit",
    "epic_id",
    "issue_id",
    "created_at",
    "updated_at",
    "archived_at"
  ],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
      "description": "Kebab-case identifier matching the work unit directory name"
    },
    "title": {
      "type": "string",
      "description": "Human-readable title"
    },
    "description": {
      "type": "string",
      "description": "One-sentence summary"
    },
    "step": {
      "type": "string",
      "enum": ["ideate", "research", "plan", "spec", "decompose", "implement", "review"],
      "description": "Current step in the lifecycle"
    },
    "step_status": {
      "type": "string",
      "enum": ["not_started", "in_progress", "completed", "blocked"],
      "description": "Status of the current step"
    },
    "steps_sequence": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["ideate", "research", "plan", "spec", "decompose", "implement", "review"]
      },
      "minItems": 7,
      "maxItems": 7,
      "description": "Fixed ordered sequence of all steps"
    },
    "deliverables": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["status", "assigned_to", "wave", "corrections"],
        "properties": {
          "status": {
            "type": "string",
            "enum": ["not_started", "in_progress", "completed", "blocked"]
          },
          "assigned_to": {
            "type": ["string", "null"]
          },
          "wave": {
            "type": "integer",
            "minimum": 0
          },
          "corrections": {
            "type": "integer",
            "minimum": 0
          }
        },
        "additionalProperties": false
      },
      "description": "Map of deliverable names to their status objects"
    },
    "gates": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["boundary", "outcome", "decided_by", "evidence", "timestamp"],
        "properties": {
          "boundary": {
            "type": "string",
            "pattern": "^(ideate|research|plan|spec|decompose|implement|review)->(ideate|research|plan|spec|decompose|implement|review)$",
            "description": "Format: {from_step}->{to_step}"
          },
          "outcome": {
            "type": "string",
            "enum": ["pass", "fail", "conditional"]
          },
          "decided_by": {
            "type": "string",
            "enum": ["manual", "evaluated", "prechecked"]
          },
          "evidence": {
            "type": "string",
            "minLength": 1,
            "description": "One-line summary of proof or path to gate file"
          },
          "conditions": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Present only when outcome is conditional"
          },
          "timestamp": {
            "type": "string",
            "format": "date-time",
            "description": "ISO 8601 timestamp"
          }
        },
        "additionalProperties": false,
        "if": {
          "properties": {
            "outcome": { "const": "conditional" }
          }
        },
        "then": {
          "required": ["conditions"]
        }
      },
      "description": "Append-only array of gate records"
    },
    "force_stop_at": {
      "type": ["string", "null"],
      "description": "Step name to prevent auto-advance at, or null"
    },
    "branch": {
      "type": ["string", "null"],
      "description": "Git branch name or null"
    },
    "mode": {
      "type": "string",
      "enum": ["code", "research"],
      "description": "Mirrors definition.yaml mode field"
    },
    "base_commit": {
      "type": "string",
      "description": "Git SHA at work unit creation"
    },
    "epic_id": {
      "type": ["string", "null"],
      "description": "Beans epic ID or null"
    },
    "issue_id": {
      "type": ["string", "null"],
      "description": "Beans issue ID or null"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 creation timestamp"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 last-mutation timestamp"
    },
    "archived_at": {
      "type": ["string", "null"],
      "description": "ISO 8601 archive timestamp or null"
    },
    "source_todo": {
      "type": ["string", "null"],
      "pattern": "^[a-z][a-z0-9]*(-[a-z0-9]+)*$",
      "description": "TODO entry ID this work unit was created from, or null"
    },
    "gate_policy_init": {
      "type": ["string", "null"],
      "enum": ["supervised", "delegated", "autonomous", null],
      "description": "Init-time gate policy hint for ideate step, or null"
    }
  },
  "additionalProperties": false
}
